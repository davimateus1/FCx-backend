import { checkCpfExists } from './check-cpf-exists'
import { checkEmailExists } from './check-email-exists'
import { checkLoginExists } from './check-login-exists'

export const middlewares = {
  checkCpfExists,
  checkEmailExists,
  checkLoginExists,
}
